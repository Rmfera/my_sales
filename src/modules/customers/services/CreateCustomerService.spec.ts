describe("CreateCustomerService", () => {
  it("should be able to create a new customer", () => {
    expect(5 + 5).toBe(10);
  });
  it("should not be able to create a new customer with email tha is already in use", () => {
    expect(5 + 5).toBe(10);
  });
});
